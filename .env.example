# Server Config
APP_ENV=development
APP_PORT=3030

# Database Config (PostgreSQL)
DB_HOST=
DB_USER=hris_user
DB_PASSWORD=
DB_NAME=hris_db
DB_PORT=5432
DB_SSLMODE=disable

# Redis Config
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Config
JWT_SECRET=
JWT_EXPIRATION_HOURS=24

# EMAIL SMTP Config
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
# https://myaccount.google.com/apppasswords -> Generate app password for Gmail
SMTP_PASS=
SMTP_FROM_EMAIL=
